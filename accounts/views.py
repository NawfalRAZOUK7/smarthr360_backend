from django.shortcuts import render
from rest_framework.views import APIView

# TODO: Implement:
# - RegisterView (Create user + return tokens)
# - LoginView (Authenticate user + return tokens)
# - MeView (Return current authenticated user)
# + use simplejwt's TokenRefreshView in urls
